#include <stdio.h>

extern double average();

int main() {
    printf("Welcome to shitass maintained by my asshole\n");

    double randomNumber = average();

    printf("The driver has received this number %lf and will keep it for future use.\n", randomNumber);
    printf("Have a great day.\nA zero will be sent to the operating system as a signal of a successful execution.\n");

    return 0;
}